
# TODO we can avoid trasshign `z` if we use a custom list instead

#info
#   pushes a variable onto the stack

#trashed
#   z

# create list named `stack` if it does not exist already
SetUpEditor [list]stack

# get length of stack
dim([list]stack)->z

# append
a->[list]stack(z+1)
