
# TODO this doesn't work

#info
#   pushes a str variable onto the stack

#trashed
#   z

# create list named `stack` if it does not exist already
SetUpEditor [list]stack

# get length of stack
dim([list]stack)->z

# define the str if undefined as to not run into an error later on
SetUpEditor STR0

# append
STR0->[list]stack(z+1)
